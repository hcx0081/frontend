const arr = ['a', 'b', 'c'];
let en = arr.entries();


for (const element of en) {
    console.log(element);
}
// a
// b
// c


const array = ['a', 'b', 'c'];

for (const arr of array) {
    console.log(arr);
}
// a
// b
// c
